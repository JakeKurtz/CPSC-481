<Window x:Class="CPSC_481.HelpPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CPSC_481"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Title="Help Page" Height="640" Width="360"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}">
    <Window.Resources>
        <local:StateColorSelector x:Key="StateColorSelector"/>
        <Style TargetType="TextBox">
            <Style.Triggers>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="224*" />
            <RowDefinition Height="26*"/>
            <RowDefinition Height="294*"/>
            <RowDefinition Height="65*" />
        </Grid.RowDefinitions>
        <!-- Menu Buttons -->
        <Grid Grid.Row="3" Background="White">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Button Style="{DynamicResource MaterialDesignToolButton}" Grid.Row="2" Grid.Column="0" Height="Auto" Margin="0,0,0,0">
                <StackPanel>
                    <materialDesign:PackIcon Kind="Food" Width="25" Height="25" HorizontalAlignment="Center"/>
                    <TextBlock>Food</TextBlock>
                </StackPanel>
            </Button>
            <Button Style="{DynamicResource MaterialDesignToolButton}" Grid.Row="1" Grid.Column="1" Height="Auto" Margin="0,0,0,0">
                <StackPanel>
                    <materialDesign:PackIcon Kind="GlassCocktail" Width="25" Height="25" HorizontalAlignment="Center"/>
                    <TextBlock>Drinks</TextBlock>
                </StackPanel>
            </Button>
            <Button Style="{DynamicResource MaterialDesignToolButton}" Grid.Row="1" Grid.Column="2" Height="Auto" Margin="0,0,0,0">
                <StackPanel>
                    <materialDesign:PackIcon Kind="NoteTextOutline" Width="25" Height="25" HorizontalAlignment="Center"/>
                    <TextBlock>Order</TextBlock>
                </StackPanel>
            </Button>
            <Button Style="{DynamicResource MaterialDesignToolButton}" Grid.Row="1" Grid.Column="3" Height="Auto" Margin="0,0,0,0">
                <StackPanel>
                    <materialDesign:PackIcon Kind="HelpCircleOutline" Width="25" Height="25" HorizontalAlignment="Center"/>
                    <TextBlock>Help</TextBlock>
                </StackPanel>
            </Button>
        </Grid>
        <Canvas Background="WhiteSmoke" Grid.Row="0" Grid.RowSpan="3"/>
        <!-- help page -->
        <StackPanel Margin="0,35,20,0" Grid.RowSpan="3">
            <!-- getting started -->
            <Expander x:Name="gettingStarted" Width="333" Background="WhiteSmoke" RenderTransformOrigin="0.5,0.5" Margin="0">
                <Expander.Header>
                    <TextBlock Style="{DynamicResource MaterialDesignHeadline6TextBlock}" Text="GETTING STARTED"/>
                </Expander.Header>
            </Expander>
            <!-- FAQ-->
            <Expander x:Name="FAQ" Width="333" Background="WhiteSmoke" RenderTransformOrigin="0.5,0.5" Margin="0">
                <Expander.Header>
                    <TextBlock Style="{DynamicResource MaterialDesignHeadline6TextBlock}" Text="FAQ"/>
                </Expander.Header>
                <ListView Name="_QnA" Margin="15,0,10,0" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Height="320" >

                </ListView>



            </Expander>
            <!-- CONTACT SERVER -->
            <Expander x:Name="contactServer" Width="333" Background="WhiteSmoke" RenderTransformOrigin="0.5,0.5" Margin="0">
                <Expander.Header>
                    <TextBlock Style="{DynamicResource MaterialDesignHeadline6TextBlock}" Text="CONTACT SERVER"/>
                </Expander.Header>
                <StackPanel>
                    <TextBlock Name ="csIntro" TextWrapping="Wrap" Margin="43,30,0,0">
                        <Bold>Optional: </Bold> add a message with your request.
                    </TextBlock>

                    <TextBox Name ="csInput"  Margin="25, 0" TextWrapping="Wrap" MinHeight="150" MaxHeight="150" MinWidth="250" MaxWidth="250" BorderThickness="1"/>
                     
                    <Button Width="200" Margin="20" Background="DarkGray" BorderBrush="LightGray" TextElement.Foreground="{DynamicResource MaterialDesignBody}"> REQUEST SERVER</Button>

                </StackPanel>
                
            </Expander>
        </StackPanel>
    </Grid>
</Window>
